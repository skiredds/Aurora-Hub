# v1.10.2.1 - ESP Fix

## 🟢 Added:

#### ESP tab: 
* **Config Persistence** — Added unique flags to all ESP toggles and color pickers, allowing your settings to be saved and loaded automatically.

## 🟡 Changed:

* **Billboard Health** — HP display now shows in percentages (e.g., "100%") instead of flat values for better readability.

## 🔴 Removed:

- Nothing

## 🔵 Fixed:

- **Healthbar ESP** — Resolved critical initialization bugs and fixed the toggle logic. The healthbar now correctly disappears when disabled and functions independently of the Billboard settings.
